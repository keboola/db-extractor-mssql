{
    "name": "keboola/db-extractor-mssql",
    "license": "MIT",
    "type": "project",
    "description": "Keboola Microsoft SQL DB Writer",
    "authors": [
        {
            "name": "keboola",
            "email": "devel@keboola.com"
        }
    ],
    "require": {
        "keboola/db-extractor-common": "^9.1.3",
        "keboola/php-utils": "^2.3.1"
    },
    "require-dev": {
        "phpunit/phpunit": "4.6.*",
        "keboola/coding-standard": "^2.0",
        "phpstan/phpstan-shim": "^0.9.2"
    },
    "autoload": {
        "psr-4": {
            "Keboola\\": "src/Keboola/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Keboola\\DbExtractor\\Tests\\": "tests/Keboola/DbExtractor"
        }
    },
    "scripts": {
        "tests": "./wait-for-it.sh mssql:1433 -t 30 -- phpunit",
        "phpstan": "phpstan analyse --no-progress --level=max ./src ./tests -c phpstan.neon",
        "phpcs": "phpcs -n --ignore=vendor --extensions=php .",
        "phpcbf": "phpcbf -n --ignore=vendor --extensions=php .",
        "build": [
            "@phpcs",
            "@phpstan",
            "@tests"
        ],
        "ci": [
            "@composer validate --no-check-all --strict",
            "@build"
        ]
    },
    "config": {
        "sort-packages": true
    }
}
